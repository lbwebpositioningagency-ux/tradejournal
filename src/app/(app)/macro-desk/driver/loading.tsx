/* Lo stesso scheletro di prima, che questa sezione ereditava dall'indice del
   desk: l'indice è passato nel gruppo `(indice)` perché il suo `loading.tsx`
   non stia più sopra `[id]`. Nessun cambiamento di aspetto. */
export { default } from "../(indice)/loading";
