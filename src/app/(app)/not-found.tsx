import { NotFoundState } from "@/components/not-found-state";

/** 404 dentro la cornice dell'app: sidebar e topbar restano leggibili. */
export default function AppNotFound() {
  return <NotFoundState />;
}
